module.exports = {generateHtml};
let htmlText = [];

function generateHtml(company) {
    // parse the company array
    for (const [key, value] of company) {

    }
    
    // add a div to the HTML code for each object in array
    
    // return the text
    return `${htmlText.join("").toString()}`
}